services:
  bullet:
    image: ghcr.io/silverbulletmd/silverbullet:2.10.0
    container_name: bullet
    restart: unless-stopped
    environment:
    - SB_AUTH=none
    volumes:
      - ./files:/space
    ports:
      - 3000:3000
